name: Scheduled Job

on:
  schedule:
    - cron: '* 11 * * *'  # Schedule at 12 PM UTC

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run script
      run: |
        /weatherInfo//rx_poc.sh
